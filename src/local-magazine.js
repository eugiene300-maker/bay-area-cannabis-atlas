'use strict';
(()=>{const q=document.querySelector('#place-search'),d=document.querySelector('#place-district'),k=document.querySelector('#place-kind');if(!q||!d||!k)return;const cards=[...document.querySelectorAll('[data-place]')];function update(){let count=0;const term=q.value.toLowerCase().trim();for(const card of cards){const match=card.dataset.search.includes(term)&&(d.value==='all'||card.dataset.district===d.value)&&(k.value==='all'||card.dataset.kind===k.value);card.hidden=!match;if(match)count++;}document.querySelector('#place-count').textContent=count+' place'+(count===1?'':'s');document.querySelector('#place-empty').hidden=count!==0;}q.addEventListener('input',update);d.addEventListener('change',update);k.addEventListener('change',update);})();

(()=>{const h=document.querySelector('[data-edition-end]');if(h&&new Date().toISOString().slice(0,10)>h.dataset.editionEnd){const p=document.createElement('p');p.className='notice';p.textContent='The featured edition is now an archive. Check current venue notices before making a plan.';h.prepend(p);}})();
